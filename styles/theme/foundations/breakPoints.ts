/* Defining the breakpoints for the application. */
const breakPoints = {
    sm: '30em',
    md: '48em',
    lg: '62em',
    xl: '80em',
    '2xl': '96em'
};

/* Exporting the breakPoints object. */
export default breakPoints;