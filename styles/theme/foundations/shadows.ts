/* Defining a constant called shadows that is an object with a property called largeSoft that is a
string. */
const shadows = {
    largeSoft: 'rgba(60, 64, 67, 0.15) 0px 2px 10px 6px;'
};

/* Exporting the shadows object. */
export default shadows;